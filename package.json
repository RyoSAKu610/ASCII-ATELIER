{
  "name": "pocket-mine",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A Google-powered airport battery screening demo for the Pocket Mine hackathon pitch.",
  "scripts": {
    "dev": "node scripts/server.mjs",
    "start": "node scripts/server.mjs",
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/analyzer.js && node --check src/export.js && node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
